const config = {
  botId: process.env.BOT_ID,
  botPassword: process.env.BOT_PASSWORD,
  sharepointHost: process.env.SHAREPOINT_HOSTNAME,
  sharepointSite: process.env.SHAREPOINT_SITE_URL,
  sharepointList: process.env.SHAREPOINT_LIST_NAME
};

export default config;
